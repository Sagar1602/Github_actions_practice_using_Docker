name: 03-workflow-runners
on: push
jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: printing
        run: |
          echo "This job is running on an Ubuntu runner."
          echo " runs-on OS: ${{github.runners}}"